diff --git a/source/visualization/Vtk/include/private/G4VtkPolydataPipeline.hh b/source/visualization/Vtk/include/private/G4VtkPolydataPipeline.hh
--- a/source/visualization/Vtk/include/private/G4VtkPolydataPipeline.hh
+++ b/source/visualization/Vtk/include/private/G4VtkPolydataPipeline.hh
@@ -91,6 +91,12 @@ class G4VtkPolydataPipeline : public G4VVtkPipeline
       filters;  // derived types can store filters in this vector
     vtkSmartPointer<vtkPolyDataMapper> mapper;  // polydagta mapper
     vtkSmartPointer<vtkActor> actor;  // all pipelines require an actor
+
+    vtkSmartPointer<vtkPoints> edgePolydataPoints;
+    vtkSmartPointer<vtkCellArray> edgePolydataLines;
+    vtkSmartPointer<vtkPolyData> edgePolydata;
+    vtkSmartPointer<vtkPolyDataMapper> edgeMapper;
+    vtkSmartPointer<vtkActor> edgeActor;
 };
 
 #endif  // G4VTKPOLYDATAPIPELINE_HH
diff --git a/source/visualization/Vtk/src/G4VtkClipClosedSurfacePipeline.cc b/source/visualization/Vtk/src/G4VtkClipClosedSurfacePipeline.cc
--- a/source/visualization/Vtk/src/G4VtkClipClosedSurfacePipeline.cc
+++ b/source/visualization/Vtk/src/G4VtkClipClosedSurfacePipeline.cc
@@ -140,4 +140,4 @@ void G4VtkClipClosedSurfacePipeline::Enable()
 void G4VtkClipClosedSurfacePipeline::Disable()
 {
   actor->SetVisibility(0);
-}
\ No newline at end of file
+}
diff --git a/source/visualization/Vtk/src/G4VtkClipOpenPipeline.cc b/source/visualization/Vtk/src/G4VtkClipOpenPipeline.cc
--- a/source/visualization/Vtk/src/G4VtkClipOpenPipeline.cc
+++ b/source/visualization/Vtk/src/G4VtkClipOpenPipeline.cc
@@ -132,4 +132,4 @@ void G4VtkClipOpenPipeline::Enable()
 void G4VtkClipOpenPipeline::Disable()
 {
   actor->SetVisibility(0);
-}
\ No newline at end of file
+}
diff --git a/source/visualization/Vtk/src/G4VtkPolydataInstanceTensorPipeline.cc b/source/visualization/Vtk/src/G4VtkPolydataInstanceTensorPipeline.cc
--- a/source/visualization/Vtk/src/G4VtkPolydataInstanceTensorPipeline.cc
+++ b/source/visualization/Vtk/src/G4VtkPolydataInstanceTensorPipeline.cc
@@ -25,6 +25,7 @@
 
 #include "G4VtkPolydataInstanceTensorPipeline.hh"
 
+#include "G4ViewParameters.hh"
 #include "G4VtkViewer.hh"
 #include "G4VtkVisContext.hh"
 
@@ -88,6 +89,19 @@ G4VtkPolydataInstanceTensorPipeline::G4VtkPolydataInstanceTensorPipeline(G4Strin
   actor->SetMapper(mapper);
   actor->SetVisibility(1);
 
+  // Tensor glyphs are instanced in the actor output, so the separate edgeActor
+  // inherited from G4VtkPolydataPipeline cannot follow each glyph correctly.
+  // Draw edges on the tensor actor itself for hlhsr instead.
+  if (vc.fDrawingStyle == G4ViewParameters::hlhsr) {
+    if (edgeActor != nullptr) {
+      vc.fViewer->renderer->RemoveActor(edgeActor);
+      edgeActor = nullptr;
+    }
+    actor->GetProperty()->SetRepresentationToSurface();
+    actor->GetProperty()->EdgeVisibilityOn();
+    actor->GetProperty()->SetEdgeColor(0., 0., 0.);
+  }
+
   // shading parameters
   actor->GetProperty()->SetAmbient(0.2);
   actor->GetProperty()->SetDiffuse(0.7);
@@ -102,4 +116,4 @@ void G4VtkPolydataInstanceTensorPipeline::Print()
 {
   G4cout << "G4VtkPolydataInstanceTensorPipeline " << GetName() << G4endl;
   G4VtkPolydataPipeline::Print();
-}
\ No newline at end of file
+}
diff --git a/source/visualization/Vtk/src/G4VtkPolydataPipeline.cc b/source/visualization/Vtk/src/G4VtkPolydataPipeline.cc
--- a/source/visualization/Vtk/src/G4VtkPolydataPipeline.cc
+++ b/source/visualization/Vtk/src/G4VtkPolydataPipeline.cc
@@ -34,9 +34,13 @@
 #include "G4VtkVisContext.hh"
 
 #include <vtkActor.h>
+#include <vtkCellArray.h>
 #include <vtkCleanPolyData.h>
+#include <vtkIdList.h>
 #include <vtkLine.h>
 #include <vtkMatrix4x4.h>
+#include <vtkPoints.h>
+#include <vtkPolyData.h>
 #include <vtkPolyDataAlgorithm.h>
 #include <vtkPolyDataMapper.h>
 #include <vtkPolyDataNormals.h>
@@ -115,19 +119,47 @@ G4VtkPolydataPipeline::G4VtkPolydataPipeline(G4String nameIn, const G4VtkVisCont
   else if (vc.fDrawingStyle == G4ViewParameters::wireframe) {
     actor->GetProperty()->SetRepresentationToWireframe();
   }
+  else if (vc.fDrawingStyle == G4ViewParameters::hlhsr) {
+    edgePolydataPoints = vtkSmartPointer<vtkPoints>::New();
+    edgePolydataLines = vtkSmartPointer<vtkCellArray>::New();
+    edgePolydata = vtkSmartPointer<vtkPolyData>::New();
+    edgePolydata->SetPoints(edgePolydataPoints);
+    edgePolydata->SetLines(edgePolydataLines);
+
+    edgeMapper = vtkSmartPointer<vtkPolyDataMapper>::New();
+    edgeMapper->SetInputData(edgePolydata);
+    edgeMapper->ScalarVisibilityOff();
+
+    edgeActor = vtkSmartPointer<vtkActor>::New();
+    edgeActor->SetMapper(edgeMapper);
+    edgeActor->SetVisibility(1);
+    edgeActor->GetProperty()->SetColor(0.0, 0.0, 0.0);
+    edgeActor->GetProperty()->SetOpacity(1.0);
+    edgeActor->GetProperty()->SetLineWidth(1.5);
+    edgeActor->GetProperty()->SetRenderLinesAsTubes(true);
+  }
 
   // add to renderer
   vc.fViewer->renderer->AddActor(GetActor());
+  if (edgeActor != nullptr) {
+    vc.fViewer->renderer->AddActor(edgeActor);
+  }
 }
 
 void G4VtkPolydataPipeline::Enable()
 {
   actor->SetVisibility(1);
+  if (edgeActor != nullptr) {
+    edgeActor->SetVisibility(1);
+  }
 }
 
 void G4VtkPolydataPipeline::Disable()
 {
   actor->SetVisibility(0);
+  if (edgeActor != nullptr) {
+    edgeActor->SetVisibility(0);
+  }
 }
 
 void G4VtkPolydataPipeline::Print()
@@ -151,6 +183,9 @@ void G4VtkPolydataPipeline::Modified()
 
 void G4VtkPolydataPipeline::Clear()
 {
+  if (edgeActor != nullptr) {
+    renderer->RemoveActor(edgeActor);
+  }
   renderer->RemoveActor(actor);
   G4VVtkPipeline::Clear();
 }
@@ -176,6 +211,26 @@ void G4VtkPolydataPipeline::SetPolydata(const G4Polyhedron& polyhedron)
     polydataCells->InsertNextCell(poly);
 
   } while (notLastFace);
+
+  if (edgeActor != nullptr) {
+    G4Point3D p1, p2;
+    G4int edgeFlag = 0;
+    const G4bool drawAuxiliaryEdges = vc.fViewer->GetViewParameters().IsAuxEdgeVisible();
+
+    do {
+      notLastFace = polyhedron.GetNextEdge(p1, p2, edgeFlag);
+      if (!drawAuxiliaryEdges && edgeFlag <= 0) {
+        continue;
+      }
+
+      const vtkIdType id1 = edgePolydataPoints->InsertNextPoint(p1.x(), p1.y(), p1.z());
+      const vtkIdType id2 = edgePolydataPoints->InsertNextPoint(p2.x(), p2.y(), p2.z());
+      auto line = vtkSmartPointer<vtkIdList>::New();
+      line->InsertNextId(id1);
+      line->InsertNextId(id2);
+      edgePolydataLines->InsertNextCell(line);
+    } while (notLastFace);
+  }
 }
 
 void G4VtkPolydataPipeline::SetPolydata(vtkPolyData *polydataIn) {
@@ -221,6 +276,9 @@ void G4VtkPolydataPipeline::SetActorTransform(G4double dx, G4double dy, G4double
                                r20, r21, r22, dz, 0.,  0.,  0.,  1.};
   transform->DeepCopy(transformArray);
   actor->SetUserMatrix(transform);
+  if (edgeActor != nullptr) {
+    edgeActor->SetUserMatrix(transform);
+  }
 }
 
 void G4VtkPolydataPipeline::SetActorColour(G4double r, G4double g, G4double b, G4double a)
diff --git a/source/visualization/Vtk/src/G4VtkStore.cc b/source/visualization/Vtk/src/G4VtkStore.cc
--- a/source/visualization/Vtk/src/G4VtkStore.cc
+++ b/source/visualization/Vtk/src/G4VtkStore.cc
@@ -409,22 +409,32 @@ void G4VtkStore::AddPrimitiveSeparate(const G4Polyhedron& polyhedron, const G4Vt
     return;
   }
 
-  auto hash = G4VtkPolydataPipeline::MakeHash(polyhedron, vc);
+  // Some polyhedra arrive with their local geometry offset away from the
+  // logical-volume origin (for example imported boolean GDML solids). Mirror
+  // the tensor pipeline here so the separate actor is transformed around the
+  // centred polydata rather than around an offset mesh.
+  auto centre = polyhedron.vertexUnweightedMean();
+  auto transform = G4Translate3D(-centre.x(), -centre.y(), -centre.z());
+
+  auto polyhedronNew = G4Polyhedron(polyhedron);
+  auto fTransformNew = G4Transform3D(vc.fTransform * transform.inverse());
+
+  polyhedronNew.Transform(transform);
+
+  auto hash = G4VtkPolydataPipeline::MakeHash(polyhedronNew, vc);
 
   auto pl = std::make_shared<G4VtkPolydataPipeline>(G4String("name"), vc);
-  pl->SetPolydata(polyhedron);
+  pl->SetPolydata(polyhedronNew);
 
   separatePipeMap.insert(std::make_pair(hash, pl));
 
   const G4VisAttributes* pVA =
     vc.fViewer->GetApplicableVisAttributes(polyhedron.GetVisAttributes());
   G4Color colour = pVA->GetColour();
-  G4Transform3D fInvObjTrans = vc.fTransform.inverse();
-
-  pl->SetActorTransform(vc.fTransform.dx(), vc.fTransform.dy(), vc.fTransform.dz(),
-                        fInvObjTrans.xx(), fInvObjTrans.xy(), fInvObjTrans.xz(),
-                        fInvObjTrans.yx(), fInvObjTrans.yy(), fInvObjTrans.yz(),
-                        fInvObjTrans.zx(), fInvObjTrans.zy(), fInvObjTrans.zz());
+  pl->SetActorTransform(fTransformNew.dx(), fTransformNew.dy(), fTransformNew.dz(),
+                        fTransformNew.xx(), fTransformNew.xy(), fTransformNew.xz(),
+                        fTransformNew.yx(), fTransformNew.yy(), fTransformNew.yz(),
+                        fTransformNew.zx(), fTransformNew.zy(), fTransformNew.zz());
   pl->SetActorColour(colour.GetRed(), colour.GetGreen(), colour.GetBlue(), colour.GetAlpha());
 }
 
